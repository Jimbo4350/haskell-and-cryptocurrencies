name: hc-io
version: 0.1
build-type: Simple
cabal-version: >= 1.10

library
  hs-source-dirs: src
  exposed-modules:
    IO
  build-depends:
    base,
    containers,
    dns,
    iproute,
    network,
    random
  default-language: Haskell2010
