name: chat
version: 0.1
build-type: Simple
cabal-version: >= 1.10

library
  hs-source-dirs: src
  default-language: Haskell2010
  build-depends:
    base >= 4.9 && < 5,
    async,
    containers,
    network,
    stm
  exposed-modules:
    Chat
